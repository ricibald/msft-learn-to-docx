services:
  msft-learn-to-docx:
    build: .
    environment:
      - GITHUB_TOKEN=${GITHUB_TOKEN:-}
    volumes:
      - ./output:/output
      - msftlearn-cache:/cache
    # Override command with your URLs and options:
    #   docker compose run msft-learn-to-docx "https://learn.microsoft.com/.../paths/copilot/" --title "Guide"

volumes:
  msftlearn-cache:
